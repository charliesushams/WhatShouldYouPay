[project]
name = "whatshouldyoupay"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "playwright>=1.58.0",
    "pytest>=9.0.2",
    "pytest-playwright>=0.7.2",
    "selectolax>=0.4.7",
]

[tool.pytest.ini_options]
pythonpath = [
  ".", "src",
]